{% if pages is defined %}
    {% for page in pages %}
        <li class="footer-nav"><a class="alt-links" href="{{ app.request.basepath }}/{{ page.pagename|lower }}">{{ page.pageName|capitalize }}</a></li>
    {% endfor %}
{% endif %}
