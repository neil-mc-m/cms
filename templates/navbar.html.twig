{# the main frontend navbar #}
{% if pages is defined %}
{% for page in pages %}
    <li {% if active_page == page.pageName %}class="active"{% endif %}><a href="{{ app.request.basepath }}/{{ page.pageTemplate }}">{{ page.pageName|capitalize }}</a></li>
{% endfor %}
{% endif %}
